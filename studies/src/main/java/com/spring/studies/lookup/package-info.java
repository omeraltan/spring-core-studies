package com.spring.studies.lookup;


/**
 *
 *      @Lookup
 *      In Spring, the Lookup Method Injection is intended for use when
 *      we want to work with two beans of different lifecycle scopes (singleton, prototype etc.).
 *      Do not use this when beans are in the same lifecycle scopes.
 *
 *
 */
